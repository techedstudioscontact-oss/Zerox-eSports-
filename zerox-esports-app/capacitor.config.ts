import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'com.zerox.esports',
    appName: 'Zerox eSports',
    webDir: 'dist',
    server: {
        androidScheme: 'https'
    }
};

export default config;
